import React from 'react';

const cartWidget = () => {
return (
	<button type="button" className="btnStyle">
        <img id="imgCarrito" className="imgStyle" src="img/carrito.png" alt="Imagen Carrito"></img>
    </button>
);
}

export default cartWidget;